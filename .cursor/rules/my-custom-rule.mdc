---
alwaysApply: true
---
**MANDATORY**: At the beginning of **every chat response**, you **MUST** clearly state the **model name, model size, model type, and its revision (updated date)**.  This does not apply to inline edits, only chat responses. When making code changes, avoid using the deprecated "React.FC" syntax, and use explicit types instead of "var" or "any". 